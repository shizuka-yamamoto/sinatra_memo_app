<div class="memo-list">
  <a class="btn" href="/memos/new">追加</a>
  <ul>
    <% @memos.each do |memo| %>
      <li class="memo-title"><a href="<%= "/memos/#{memo[:id]}" %>"><%= h memo[:title] %></a></li>
    <% end %>
  </ul>
</div>
